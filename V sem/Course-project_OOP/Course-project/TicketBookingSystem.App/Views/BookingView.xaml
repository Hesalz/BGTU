<UserControl x:Class="TicketBookingSystem.App.Views.BookingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="800" Height="558">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding EventPhotos.Count}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="6">
                <Grid>
                    <Image Source="{Binding CurrentPhoto, Converter={StaticResource ByteArrayToImageSourceConverter}}"
                           Stretch="Uniform" />
                    <Button Command="{Binding PreviousPhotoCommand}"
                            Width="36" Height="36" HorizontalAlignment="Left" VerticalAlignment="Center"
                            Margin="10" Background="#66000000" BorderBrush="Transparent">
                        <Path Data="M20,4 L8,12 L20,20" Stroke="White" StrokeThickness="2" StrokeEndLineCap="Round" StrokeLineJoin="Round"/>
                    </Button>
                    <Button Command="{Binding NextPhotoCommand}"
                            Width="36" Height="36" HorizontalAlignment="Right" VerticalAlignment="Center"
                            Margin="10" Background="#66000000" BorderBrush="Transparent">
                        <Path Data="M8,4 L20,12 L8,20" Stroke="White" StrokeThickness="2" StrokeEndLineCap="Round" StrokeLineJoin="Round"/>
                    </Button>
                </Grid>
            </Border>

        </Grid>

        <StackPanel Grid.Column="1" MaxWidth="500">
            <TextBlock Text="Бронирование билетов" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="{Binding SelectedEvent.Title}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding SelectedEvent.Description}" TextWrapping="Wrap" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                <TextBlock Text="Дата: " FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedEvent.Date, StringFormat='{}{0:dd.MM.yyyy}'}"/>
                <TextBlock Text=" Время: " FontWeight="Bold" Margin="10,0,0,0"/>
                <TextBlock Text="{Binding SelectedEvent.Time, StringFormat='{}{0:hh\\:mm}'}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Место: " FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedEvent.Location}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Доступно билетов: " FontWeight="Bold"/>
                <TextBlock Text="{Binding AvailableTickets}"/>
            </StackPanel>

            <TextBlock Text="Количество билетов:" Margin="0,10,0,5"/>
            <TextBox Text="{Binding TicketCount, UpdateSourceTrigger=PropertyChanged}" 
                    Style="{StaticResource ModernTextBoxStyle}"
                    Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Итого: " FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalAmount, StringFormat='{}{0:C}'}" FontSize="18" FontWeight="Bold"/>
            </StackPanel>

            <Border Background="#FFF5F5"
                   BorderBrush="{StaticResource DangerColor}"
                   BorderThickness="2"
                   CornerRadius="8"
                   Padding="15"
                   Margin="0,0,0,15"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
                <Border.Effect>
                    <DropShadowEffect Color="#EF4444" BlurRadius="8" ShadowDepth="0" Opacity="0.2"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal" Height="44">
                    <TextBlock Text="⚠️" 
                              FontSize="20"
                              Margin="0,0,10,0"
                              VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ErrorMessage}" 
                              Foreground="{StaticResource DangerColor}" 
                              FontWeight="SemiBold"
                              FontSize="14"
                              TextWrapping="Wrap"
                              VerticalAlignment="Center"
                              MaxWidth="400"/>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Command="{Binding CancelBookingCommand}" 
                        Margin="0,0,10,0" Padding="20,10"/>
                <Button Content="Подтвердить бронирование" Command="{Binding ConfirmBookingCommand}" 
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                        Padding="20,10"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

