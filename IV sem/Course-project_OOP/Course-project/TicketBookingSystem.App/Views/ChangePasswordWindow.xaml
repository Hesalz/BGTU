<Window x:Class="TicketBookingSystem.App.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d" 
        Title="Смена пароля" 
        Height="620" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource CardBackgroundColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryLightColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="30">
        <StackPanel>
            <TextBlock Text="Смена пароля" 
                      Style="{StaticResource TitleTextStyle}" 
                      Margin="0,0,0,30"
                      HorizontalAlignment="Center"/>
            
            <TextBlock Text="Текущий пароль:" Style="{StaticResource LabelTextStyle}"/>
            <PasswordBox x:Name="OldPasswordBox" 
                       PasswordChanged="OldPasswordBox_PasswordChanged" 
                       Margin="0,0,0,20"/>
            
            <TextBlock Text="Новый пароль:" Style="{StaticResource LabelTextStyle}"/>
            <PasswordBox x:Name="NewPasswordBox" 
                       PasswordChanged="NewPasswordBox_PasswordChanged" 
                       Margin="0,0,0,20"/>
            
            <TextBlock Text="Подтверждение нового пароля:" Style="{StaticResource LabelTextStyle}"/>
            <PasswordBox x:Name="ConfirmNewPasswordBox" 
                       PasswordChanged="ConfirmNewPasswordBox_PasswordChanged" 
                       Margin="0,0,0,25"/>
            
            <TextBlock Text="{Binding PasswordErrorMessage}" 
                      Style="{StaticResource ErrorTextStyle}"
                      Visibility="{Binding PasswordErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                      Margin="0,0,0,15"
                      TextWrapping="Wrap"/>
            
            <TextBlock Text="{Binding PasswordSuccessMessage}" 
                      Foreground="{StaticResource SuccessColor}" 
                      Margin="0,0,0,20" 
                      TextWrapping="Wrap" 
                      FontWeight="Bold"
                      FontSize="14"
                      Visibility="{Binding PasswordSuccessMessage, Converter={StaticResource StringToVisibilityConverter}}"
                      HorizontalAlignment="Center"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Отмена" 
                       Click="CancelButton_Click"
                       Style="{StaticResource SecondaryButtonStyle}"
                       Margin="0,0,10,0"
                       Padding="20,10"/>
                <Button Content="Изменить пароль" 
                       Command="{Binding ChangePasswordCommand}"
                       Padding="20,10"
                       IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>

