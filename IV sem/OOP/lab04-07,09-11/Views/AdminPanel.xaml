<Window x:Class="Lab4_5.AdminPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab4_5"
        xmlns:converters="clr-namespace:Lab4_5.Converters"
        mc:Ignorable="d"
        Title="{DynamicResource Title}" 
        Height="700" Width="1234"
        WindowStyle="ThreeDBorderWindow" 
        Background="{DynamicResource BackgroundBrush}" 
        BorderBrush="{DynamicResource BorderBrush}" 
        Icon="/logo.png">

    <Window.Resources>
        <converters:ByteArrayToImageConverter x:Key="ImageConverter"/>

        <Style TargetType="TextBox" x:Key="DefaultTextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="CategoryComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width" Value="130"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Foreground="{TemplateBinding Foreground}"
                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                        ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                        Margin="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Left"/>
                                    <Path x:Name="Arrow"
                                        Grid.Column="1"
                                        Fill="{DynamicResource SecondaryTextColor}"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Data="M 0 0 L 4 4 L 8 0 Z"/>
                                </Grid>
                            </ToggleButton>

                            <Popup x:Name="Popup"
                                AllowsTransparency="True"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                    Background="{DynamicResource ControlBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="0,0,4,4"
                                    Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Arrow" Property="Data" Value="M 0 4 L 4 0 L 8 4 Z"/>
                                <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource HighlightBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToggleButton" Property="BorderBrush" Value="{DynamicResource HighlightBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0">
                            <ContentPresenter VerticalAlignment="Center"
                                HorizontalAlignment="Left"
                                Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource SelectedItemBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource DefaultTextBox}">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="{DynamicResource HighlightBrush}"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            SnapsToDevicePixels="True">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                    Focusable="False"
                                    HorizontalScrollBarVisibility="Hidden"
                                    VerticalScrollBarVisibility="Hidden"
                                    VerticalAlignment="Center"
                                    Margin="8,0,8,0"/>

                                <TextBlock x:Name="placeholderText"
                                    Text="{DynamicResource SearchPlaceholder}"
                                    Foreground="{DynamicResource SecondaryTextColor}"
                                    Margin="8,0"
                                    VerticalAlignment="Center"
                                    Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>

                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource HighlightBrush}"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource HighlightBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="DefaultButton">
            <Setter Property="Background" Value="{DynamicResource DarkRed}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="contentPresenter"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource LightRed}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource LightRed}"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SelectedItemBackground}"/>
                                <Setter TargetName="contentPresenter" Property="Margin" Value="1,1,0,0"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ListBoxItem" x:Key="CinemaItemStyle">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SelectedItemBackground}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" VerticalAlignment="Top" Background="{DynamicResource MenuBackground}">
            <MenuItem Header="{DynamicResource ManageCinemas}" Foreground="White">
                <MenuItem Header="{DynamicResource AddCinema}" Click="AddCinema_Click" Background="{DynamicResource MenuBackground}"/>
                <MenuItem Header="{DynamicResource EditCinema}" Click="EditCinema_Click" Background="{DynamicResource MenuBackground}"/>
                <MenuItem Header="{DynamicResource DeleteCinema}" Click="DeleteCinema_Click" Background="{DynamicResource MenuBackground}"/>
                <Separator/>
                <MenuItem Header="{DynamicResource Refresh}" Click="RefreshList_Click" Background="{DynamicResource MenuBackground}"/>
                <Separator/>
                <MenuItem Header="{DynamicResource Undo}" Click="Undo_Click" Background="{DynamicResource MenuBackground}"/>
                <MenuItem Header="{DynamicResource Redo}" Click="Redo_Click" Background="{DynamicResource MenuBackground}"/>
            </MenuItem>
        </Menu>

        <Border Grid.Row="0" Background="{DynamicResource SecondaryBackground}" Padding="5" Margin="0,18,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="-5,0,0,0">
                <TextBox x:Name="SearchTextBox" 
                    Style="{StaticResource SearchTextBoxStyle}"
                    TextChanged="SearchTextBox_TextChanged"
                    ToolTip="{DynamicResource SearchPlaceholder}"
                    Width="180"/>

                <ComboBox x:Name="CategoryFilter" 
                    Style="{StaticResource CategoryComboBoxStyle}"
                    ItemContainerStyle="{StaticResource ComboBoxItemStyle}"
                    DisplayMemberPath="Name"
                    SelectedValuePath="CategoryId"
                    SelectionChanged="Filters_SelectionChanged"
                    ToolTip="{DynamicResource CategoryFilterTooltip}"
                    Width="178"/>

                <ComboBox x:Name="HallCountFilter"
                    Style="{StaticResource CategoryComboBoxStyle}"
                    Width="187"
                    SelectionChanged="Filters_SelectionChanged"
                    ToolTip="{DynamicResource HallCountFilterTooltip}"
                    SelectedIndex="0">
                    <ComboBoxItem Content="{DynamicResource AnyHallsCount}"/>
                    <ComboBoxItem>1-3</ComboBoxItem>
                    <ComboBoxItem>4-7</ComboBoxItem>
                    <ComboBoxItem>8+</ComboBoxItem>
                </ComboBox>

                <ComboBox x:Name="CapacityFilter"
                    Style="{StaticResource CategoryComboBoxStyle}"
                    SelectionChanged="Filters_SelectionChanged"
                    ToolTip="{DynamicResource CapacityFilterTooltip}"
                    SelectedIndex="0" Width="187">
                    <ComboBoxItem Content="{DynamicResource AnyCapacity}"/>
                    <ComboBoxItem Content="{DynamicResource CapacityUpTo100}"/>
                    <ComboBoxItem>100-300</ComboBoxItem>
                    <ComboBoxItem>300+</ComboBoxItem>
                </ComboBox>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Width="262">
                    <TextBlock Text="{DynamicResource RatingFilterText}" 
                        VerticalAlignment="Center" 
                        Foreground="{DynamicResource TextColor}"
                        FontSize="14"
                        Margin="0,0,5,0"
                        MinWidth="60"/>

                    <Slider x:Name="RatingFilter" 
                        Minimum="0" 
                        Maximum="5" 
                        Width="150" 
                        Margin="5,0" 
                        TickFrequency="0.5"
                        ValueChanged="RatingFilter_ValueChanged"
                        IsSnapToTickEnabled="True"
                        ToolTip="{DynamicResource RatingFilterTooltip}"
                        Background="Transparent">
                        <Slider.Resources>
                            <SolidColorBrush x:Key="SliderTrackFill" Color="{DynamicResource SecondaryTextColor}"/>
                            <SolidColorBrush x:Key="SliderTrackValueFill" Color="{DynamicResource HighlightBrush}"/>

                            <Style TargetType="Thumb">
                                <Setter Property="Background" Value="{DynamicResource HighlightBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="Width" Value="10"/>
                                <Setter Property="Height" Value="10"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Thumb">
                                            <Ellipse Fill="{TemplateBinding Background}"
                                                Stroke="{TemplateBinding BorderBrush}"
                                                StrokeThickness="1"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Slider.Resources>
                    </Slider>

                    <TextBlock Text="{Binding Value, ElementName=RatingFilter, StringFormat={}{0:F1}+}" 
                        VerticalAlignment="Center" 
                        Margin="5,0,10,0" 
                        Foreground="{DynamicResource HighlightBrush}"
                        FontWeight="SemiBold"
                        FontSize="14"
                        MinWidth="40"/>
                </StackPanel>

                <Button Content="{DynamicResource ResetButtonText}" 
                    Style="{StaticResource DefaultButton}"
                    Click="ResetFilters_Click"/>
                <Image Height="28" Source="/Image1.png" Stretch="Fill" Width="27" 
                    RenderTransformOrigin="1.683,0.509" MouseDown="Image_MouseDown" 
                    Margin="20,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="10,0,10,10" Background="{DynamicResource BackgroundBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource ControlBackgroundBrush}" CornerRadius="5" 
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="5">
                <ListBox x:Name="CinemaListBox" 
                    ScrollViewer.VerticalScrollBarVisibility="Hidden"
                    ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                    Background="{DynamicResource ControlBackgroundBrush}"
                    BorderThickness="0"
                    ItemContainerStyle="{StaticResource CinemaItemStyle}"
                    SelectionChanged="CinemaListBox_SelectionChanged">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource SecondaryBackground}" CornerRadius="3" 
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                Padding="17" Margin="0,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" 
                                        FontWeight="Bold" FontSize="16"
                                        Foreground="{DynamicResource TextColor}"/>

                                    <TextBlock Text="{Binding Address}" 
                                        Foreground="{DynamicResource SecondaryTextColor}" 
                                        Margin="0,5,0,10"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="180"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="ID:" Foreground="{DynamicResource SecondaryTextColor}" Width="40"/>
                                                <TextBlock Text="{Binding CinemaId}" Foreground="{DynamicResource TextColor}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                                <TextBlock Text="{DynamicResource Halls}" Foreground="{DynamicResource SecondaryTextColor}" Width="40"/>
                                                <TextBlock Text="{Binding HallCount}" Foreground="{DynamicResource TextColor}"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Margin="3,0,0,0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource Papas}" Foreground="{DynamicResource SecondaryTextColor}" Width="80"/>
                                                <TextBlock Text="{Binding Capacity}" Foreground="{DynamicResource TextColor}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                                <TextBlock Text="{DynamicResource RatingFilterText}" Foreground="{DynamicResource SecondaryTextColor}" Width="80"/>
                                                <TextBlock Text="{Binding Rating, StringFormat={}{0:F1}/5}" 
                                                    Foreground="{DynamicResource HighlightBrush}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>

                                    <Border Background="{DynamicResource MenuBackground}" CornerRadius="3"
                                        Margin="0,10,0,0" Padding="5,3"
                                        HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding Category.Name}" 
                                            Foreground="{DynamicResource MenuForeground}" FontSize="12"/>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Border Grid.Column="1" Margin="10,0,0,0" Background="{DynamicResource ControlBackgroundBrush}" 
                CornerRadius="5" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="DetailsPanel" Visibility="Collapsed">
                        <TextBlock FontSize="18" FontWeight="Bold" 
                            Foreground="{DynamicResource TextColor}" Margin="0,0,0,10"
                            Text="{Binding Name}"/>

                        <TextBlock FontSize="14"
                            Foreground="{DynamicResource SecondaryTextColor}" Margin="0,0,0,15"
                            Text="{Binding Address}"/>

                        <TextBlock FontSize="14"
                            Foreground="{DynamicResource SecondaryTextColor}" Margin="0,0,0,15"
                            Text="{Binding Category.Name}"/>

                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="ID:" Grid.Row="0" Grid.Column="0"
                                Foreground="{DynamicResource SecondaryTextColor}" FontWeight="SemiBold"
                                Margin="0,0,10,5"/>
                            <TextBlock Text="{Binding CinemaId}" Grid.Row="0" Grid.Column="1"
                                Foreground="{DynamicResource TextColor}" Margin="0,0,0,5"/>

                            <TextBlock Text="{DynamicResource Halls}" Grid.Row="1" Grid.Column="0"
                                Foreground="{DynamicResource SecondaryTextColor}" FontWeight="SemiBold"
                                Margin="0,0,10,5"/>
                            <TextBlock Text="{Binding HallCount}" Grid.Row="1" Grid.Column="1"
                                Foreground="{DynamicResource TextColor}" Margin="0,0,0,5"/>

                            <TextBlock Text="{DynamicResource Papas}" Grid.Row="2" Grid.Column="0"
                                Foreground="{DynamicResource SecondaryTextColor}" FontWeight="SemiBold"
                                Margin="0,0,10,5"/>
                            <TextBlock Text="{Binding Capacity}" Grid.Row="2" Grid.Column="1"
                                Foreground="{DynamicResource TextColor}" Margin="0,0,0,5"/>

                            <TextBlock Text="{DynamicResource RatingFilterText}" Grid.Row="3" Grid.Column="0"
                                Foreground="{DynamicResource SecondaryTextColor}" FontWeight="SemiBold"
                                Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Rating, StringFormat={}{0:F1}/5}" Grid.Row="3" Grid.Column="1"
                                Foreground="{DynamicResource HighlightBrush}" FontWeight="SemiBold"/>
                        </Grid>

                        <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,0,0,15"/>

                        <TextBlock Text="{DynamicResource Images}" FontSize="16" FontWeight="Bold"
                            Foreground="{DynamicResource TextColor}" Margin="0,0,0,10"/>

                        <ItemsControl x:Name="ImagesItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,10,10" CornerRadius="3">
                                        <StackPanel Width="180">
                                            <Image Source="{Binding ImageData, Converter={StaticResource ImageConverter}}" 
                                                Height="150" Stretch="UniformToFill"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock x:Name="NoImagesText" Text="{DynamicResource NoImagesText}" 
                            Foreground="{DynamicResource SecondaryTextColor}" FontStyle="Italic"
                            HorizontalAlignment="Center" Margin="0,20,0,0"
                            Visibility="Collapsed"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>